
/* CPU:
 *  - 64 bit -> current
 *  - 32 bit
 *  - 16 bit
 *  Bit size of a processor : the number of bits a CPU/processor can process at a time (one CPU cycle)
 */

import java.lang.*;

class DataTypes
{
    public static void main(String[] args)
    {
        int x =-5;
        System.out.println(Integer.toBinaryString(x));
    }
}
